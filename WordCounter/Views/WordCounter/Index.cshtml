@using WordCounter;
@{
	ViewData["Title"] = "Word Count List";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

@foreach (KeyValuePair<Guid, WordSearch> item in Model)
{
	<a href="/wordcounter/@item.Key.ToString()">
		<p>@item.Key.ToString().Substring(0, 5) -- @item.Value.Keyword , @item.Value.Count</p>
	</a>
}

<a href="/wordcounter/new" role="button" class="btn btn-success">New Word Search</a>